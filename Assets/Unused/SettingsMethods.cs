/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class SettingsMethods : MonoBehaviour
{
    SettingsList settingsList = new SettingsList();




    public void OptionMethod(string parameterSettingKey)//, bool parameterSettingValue)
    {
        //Debug.Log(parameterSettingKey + ": " + parameterSettingValue);

        // placeholderOption = !placeholderOption
        // placeholderTextMeshProUGUI.text = placeholderOption ? "ON" : "OFF";
        // Debug.Log(placeholderOption);

        print("OptionMethod Running");

        if (settingsList.settings.ContainsKey(parameterSettingKey))
        {

            print("if loop Running");
        }
        else
        {
            print("if loop not Running");
            print(parameterSettingKey);
        }
    }
}
*/